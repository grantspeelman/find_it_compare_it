<%= page_header("#{@board.try(:name)} Show item") %>

<%= render :partial => 'action_links' %>

<div class="span10">
  <p>
    <b>Url:</b>
    <%= @item.url %>
  </p>

  <p>
    <b>Notes:</b>
    <%= @item.notes %>
  </p>
</div>


